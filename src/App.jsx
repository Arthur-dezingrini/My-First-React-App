import React from 'react'
import ReactDOM from 'react-dom/client'
import RoutineAll from './RoutineAll'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RoutineAll />
  </React.StrictMode>,
)
